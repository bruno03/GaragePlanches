@model IEnumerable<GaragePlanches.Models.Car>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@*<form method="get">
        <input type="search" name="searchTerm" />
        <input type="submit" value="Search by Brand" />
   </form>*@

@* AJAX CALL
    @using (Ajax.BeginForm(
                    new AjaxOptions
                    {
                        HttpMethod = "get",
                        InsertionMode = InsertionMode.Replace,
                        UpdateTargetId = "carList"
                    }))
            {
                <input type="search" name="searchTerm" />
                <input type="submit" value="Search by Brand" />
            }
*@

    <form method="get" action="@Url.Action("Index")"
          data-gp-ajax="true" data-gp-target="#carList">

         <input type="search" name="searchTerm" data-gp-autocomplete="@Url.Action("Autocomplete")" />
         <input type="submit" value="Search by Brand" />
    </form>
<br />

@Html.Partial("_Car", Model)

